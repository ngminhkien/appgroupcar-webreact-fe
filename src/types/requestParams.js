/**
 * Common params for paged list API requests.
 * @typedef {Object} BaseListRequestParams
 * @property {string=} search
 * @property {number=} pageNumber
 * @property {number=} pageSize
 * @property {boolean=} isActive
 */

export {};
